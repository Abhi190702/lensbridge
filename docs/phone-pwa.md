# Phone PWA

The phone app uses:

- `MediaDevices.getUserMedia()`
- `RTCPeerConnection`
- WebSocket signaling
- PWA manifest

Important browser constraint: camera access usually requires HTTPS or localhost. Local HTTPS/WSS support is planned.
