# Roadmap

See the root `ROADMAP.md` for the release plan.

This document mirrors the staged approach:

- V1 phone-to-desktop preview.
- V2 Windows DirectShow camera bridge and OBS fallback reliability.
- V3 universal sources and native receiver performance work.
- V4 AI and plugins.
