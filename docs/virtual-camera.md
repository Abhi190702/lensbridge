# Virtual Camera

Virtual camera output is V2.

V1:

- Desktop preview only.

V2:

- Linux `v4l2loopback`.
- FFmpeg/GStreamer frame bridge.
- OBS fallback guides for Windows/macOS.

Future:

- Native Windows DirectShow.
- Native macOS CoreMediaIO.
