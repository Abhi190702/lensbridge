# Windows

V1 does not ship a DirectShow driver.

Use OBS Virtual Camera as the fallback path. Native Windows support requires careful driver engineering and signing, and it is not claimed as working yet.
