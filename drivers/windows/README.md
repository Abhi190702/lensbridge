# Windows Virtual Camera

V1 does not install a DirectShow driver.

Use OBS Virtual Camera as the current fallback. Native Windows camera output is future work and will not be claimed until it is real.
