# Linux Virtual Camera Example

Future Linux native pipeline setup:

```bash
cd drivers/linux
./setup-v4l2loopback.sh
```

Frame piping into `v4l2loopback` is not implemented today. Use OBS Output Mode plus OBS Virtual Camera for the current
webcam workflow.
